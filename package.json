{
  "name": "cyber-eyes-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Cyber Eyes frontend for proxy, direct, and demo deployment modes.",
  "scripts": {
    "build": "node scripts/frontend/build.mjs",
    "dev": "node scripts/frontend/server.mjs --port 3000",
    "preview": "node scripts/frontend/server.mjs --dist --port 4173",
    "start": "node scripts/frontend/server.mjs --dist --port 3000",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
